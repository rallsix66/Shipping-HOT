services:
  shipping-hot:
    build:
      context: .
      dockerfile: Dockerfile
    image: shipping-hot:local
    ports:
      - '127.0.0.1:4444:4444'
    volumes:
      - shipping_hot_data:/usr/app/.data
    environment:
      - HOST=0.0.0.0
      - PORT=4444
      - NODE_ENV=production
      - G_CLIENT_ID=
      - G_CLIENT_SECRET=
      - JWT_SECRET=
      - INIT_TABLE=true
      - ENABLE_CACHE=true

volumes:
  shipping_hot_data:
    # Preserve the existing physical volume. Renaming it is a separate data migration.
    name: newsnow_data
